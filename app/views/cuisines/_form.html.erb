<%= form_for(@cuisine) do |f| %>
  <% if @cuisine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cuisine.errors.count, "error") %> prohibited this cuisine from being saved:</h2>

      <ul>
      <% @cuisine.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :category %><br />
    <%= f.text_field :category %>
  </div>
  <div class="field">
    <%= f.label :ingredients %><br />
    <%= f.text_area :ingredients %>
  </div>
  <div class="field">
    <%= f.label :steps_to_prepare %><br />
    <%= f.text_area :steps_to_prepare %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
